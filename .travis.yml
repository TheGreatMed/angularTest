langage: node_js
node_js:
  - "10"
before_script:
  - npm install -g @angular/cli@8
jobs:
  include:
    - stage: clean
      script:
        - cd crud-angular
        - mvn clean
    - stage: build
      script:
        - cd crud-angular
        - mvn install